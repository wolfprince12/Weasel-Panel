# Windows 虚拟机测试指引（v0.1.0 preview）

给测试者的最短路径：**拷进去 → 放行 SmartScreen → 打开「环境诊断」→ 点「开始探测」→ 点「复制报告」→ 把报告贴回来**。

---

## 1. 选哪个 exe

| 文件 | 适用 | 说明 |
|---|---|---|
| `dist/win-arm64/WeaselPanel.exe` | Apple Silicon（M 系列）上的 Win11 | **首选**。原生 ARM64，性能最好 |
| `dist/win-x64/WeaselPanel.exe` | 常规 x64 电脑 | ARM 版 Win11 也能靠模拟层跑它，但启动慢 |

两者都是**自包含单文件**，不需要预先安装 .NET 运行时，双击即可运行。

## 2. 拷进虚拟机

任选其一：
- Parallels / VMware 的**共享文件夹**，或直接从 macOS 拖进 Windows 窗口
- 若拖拽失败，可挂个 U 盘或走局域网共享

> exe 有 150+ MB，因为把 .NET 8 与 WPF 运行时一起打包了。后续会做裁剪瘦身。

## 3. 首次运行：放行未签名程序

本预览版**未做代码签名**，Windows SmartScreen 会拦截：

1. 双击后若出现蓝色提示「Windows 已保护你的电脑」→ 点**「更多信息」**
2. 出现**「仍要运行」**按钮 → 点击
3. 若杀软额外拦截，选择允许/加白

这是预期行为，不是文件损坏。正式版会补签名。

## 4. 要测什么

打开后默认在**「环境诊断」**页：

### 4.1 先看环境表（不用点任何按钮）

确认这几项是否**都找到了**（没找到会显示红色）：

- 程序目录
- 共享数据目录
- 用户目录
- 部署器

⚠️ 如果你的 Windows 用户名是**中文**，请特别留意「当前用户名」与「预期管道名」两项 —— 这正是本次要验证的核心场景之一。

### 4.2 点「开始探测」

会自动跑三项（不跑部署，因为部署会改动数据）：

| 探测项 | 要确认什么 |
|---|---|
| **命名管道** | 是否显示「连通」。若失败，看详情里哪个候选名、报什么错 |
| **日志文件** | 是否找到 `rime.weasel` 开头的日志，**文件名格式**是什么 |
| **librime-lua 插件** | 是否找到 lua 插件文件 —— 决定「紫毫纠错」能否落地 |

### 4.3 确认小狼毫正在运行

命名管道探测依赖 **WeaselServer 正在运行**。测试前请先：
- 切到小狼毫输入法、随便打两个字，确认候选窗能出来
- 或看任务栏托盘有小狼毫图标

若小狼毫没运行，管道探测必然失败，这不代表面板有问题。

### 4.4 点「执行部署 /deploy」（可选，会改数据）

验证部署器退出码。会真实触发一次部署，**耗时可能几十秒**。

- 退出码 `0` = 成功
- 退出码 `1` = 按上游语义为「已有部署器实例在运行」

⚠️ 部署前建议先备份 `%APPDATA%\Rime`。

### 4.5 点「复制报告」

会把环境与全部探测结果生成一段文本到剪贴板，**直接贴回给我**即可。

## 5. 外观页（次要，但值得一看）

- 左侧是候选窗预览，颜色由 Core 按上游完整回退链计算
- 切换配色方案，看预览是否跟着变
- 调字号、改字体，看预览是否跟着变
- 点「应用设置」会写 `%APPDATA%\Rime\weasel.custom.yaml`

⚠️ **建议先只看不应用**，或应用前备份。此页尚未经过真机验证。

## 6. 出问题怎么办

- 崩溃 → 会弹出异常详情，截图发我
- 界面错位/显示异常 → 截图发我
- 任何一项探测失败 → 用「复制报告」把结果发我

日志目录（若有问题）：
- 面板日志：暂未接入文件日志，靠界面显示
- 小狼毫日志：`%TEMP%\rime.weasel\*.INFO.*`

## 7. 已知限制（v0.1.0 preview）

- 未做代码签名
- 未接入文件日志
- 外观页的「应用设置」会写用户配置，尚未充分验证
- 备份、方案管理、词库、紫毫纠错等功能尚未实现
