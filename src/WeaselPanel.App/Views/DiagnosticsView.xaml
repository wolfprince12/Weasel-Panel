<UserControl x:Class="WeaselPanel.App.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:l10n="clr-namespace:WeaselPanel.App.Localization"
             xmlns:svc="clr-namespace:WeaselPanel.App.Services">
    <StackPanel>

        <!-- ── 环境信息 ─────────────────────────────── -->
        <Border Style="{StaticResource Card}" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="{l10n:L Diag.EnvTitle}" Style="{StaticResource SectionTitle}"/>
                <ItemsControl ItemsSource="{Binding EnvironmentRows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Key}" FontSize="12.5"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="1" Text="{Binding Value}" FontSize="12.5"
                                           TextWrapping="Wrap" VerticalAlignment="Top">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                    <Setter Property="Foreground" Value="#FFC5221F"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- ── 探测 ─────────────────────────────────── -->
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="{l10n:L Diag.ProbeTitle}" Style="{StaticResource SectionTitle}"/>
                <TextBlock Text="{l10n:L Diag.ProbeIntro}"
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap" Margin="0,0,0,12"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="{l10n:L Diag.RunProbe}" Style="{StaticResource PrimaryButton}"
                            Command="{Binding RunProbeCommand}" Margin="0,0,10,0"/>
                    <Button Content="{l10n:L Diag.RunDeploy}" Style="{StaticResource SecondaryButton}"
                            Command="{Binding RunDeployCommand}" Margin="0,0,10,0"/>
                    <Button Content="{l10n:L Diag.CopyReport}" Style="{StaticResource SecondaryButton}"
                            Command="{Binding CopyReportCommand}"/>
                </StackPanel>

                <Border Background="#FFF0F2F5" CornerRadius="6" Padding="10,8" Margin="0,0,0,12">
                    <TextBlock Text="{Binding StatusText}" FontSize="12.5"
                               Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                </Border>

                <ItemsControl ItemsSource="{Binding Results}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FFFAFBFC" BorderBrush="{StaticResource BorderBrushKey}"
                                    BorderThickness="1" CornerRadius="6" Padding="12,10" Margin="0,0,0,8">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <!-- 徽标背景按 Status（枚举）判断，不按 StatusText（本地化文本）——
                                             否则用户切到英文后 "正常" 变成 "OK"，DataTrigger 全部失配、徽标变灰。 -->
                                        <Border CornerRadius="4" Padding="7,2" Margin="0,0,8,0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#FFE8EAED"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static svc:ProbeStatus.Ok}">
                                                            <Setter Property="Background" Value="#FFD9F0DC"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static svc:ProbeStatus.Warn}">
                                                            <Setter Property="Background" Value="#FFFDF0D9"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static svc:ProbeStatus.Fail}">
                                                            <Setter Property="Background" Value="#FFFBE0DF"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding StatusText}" FontSize="11" FontWeight="SemiBold"/>
                                        </Border>
                                        <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Summary}" FontSize="12.5"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding Details}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" FontSize="11.5" FontFamily="Consolas"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           TextWrapping="Wrap" Margin="0,1"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
